{% extends 'base.html.twig' %}

{% block title %}Hashtag{% endblock %}

{% block body %}
    <h1>Hashtag</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ hashtag.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ hashtag.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_hashtag_index') }}">back to list</a>

    <a href="{{ path('app_hashtag_edit', {'id': hashtag.id}) }}">edit</a>

    {{ include('hashtag/_delete_form.html.twig') }}
{% endblock %}
