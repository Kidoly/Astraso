{% extends 'base.html.twig' %}

<<<<<<< HEAD
{% block title %}Astraso
{% endblock %}

{% block stylesheets %}
	{{ parent() }}
	<style>
		/* Ajoutez vos styles personnalisés ici */
		body {
			grid-template-columns: repeat(1, 2fr) !important;
		}
		.container {
			padding: 20px;
		}
	</style>
{% endblock %}

{% block body %}
	{% for flash_error in app.flashes('verify_email_error') %}
		<div class="alert alert-danger" role="alert">{{ flash_error }}</div>
	{% endfor %}

	<h1>Inscription</h1>

	<div class="container">
		{{ form_errors(registrationForm) }}

		{{ form_start(registrationForm) }}
		{{ form_row(registrationForm.username, {
                'attr': {'class': 'form-control mb-3'}
            }) }}
		{{ form_row(registrationForm.email, {
                'attr': {'class': 'form-control mb-3'}
            }) }}
		{{ form_row(registrationForm.plainPassword, {
                'label': 'Password',
                'attr': {'class': 'form-control mb-3'}
            }) }}
		<div class="form-check mb-3">
			{{ form_row(registrationForm.agreeTerms, {
                    'label_attr': {'class': 'form-check-label'},
                    'attr': {'class': 'form-check-input'}
                }) }}
		</div>

		<button type="submit" class="btn btn-primary">S'inscrire</button>
		{{ form_end(registrationForm) }}

		<button type="submit" class="btn" id="register-button" style="background-color: var(--secondary-color);">
			<a href="{{ path('app_login') }}" style="color: black;">Vous avez déjà un compte?</a>
		</button>
	</div>
=======
{% block title %}Créer un compte{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        body {
            grid-template-columns: repeat(1, 1fr)!important;
        }
        .gradient-form {
            background-color: #eee;
        }
        .container {
            height: 100vh;
            margin: 0;
            padding: 20px;
        }
        .card {
            border-radius: 1.25rem;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.15);
        }
        .form-control {
            border-radius: 2rem;
            border: 1px solid #ced4da;
            padding: 0.75rem 1rem;
            margin-bottom: 1.5rem;
        }
        .btn-primary {
            background-color: #4e73df;
            border-radius: 1.25rem;
            padding: 0.75rem 1.5rem;
            transition: all 0.3s ease;
        }
        .btn-primary:hover {
            background-color: #3d5bb5;
        }
        .btn-outline-danger {
            color: #dc3545;
            border-color: #dc3545;
            border-radius: 2rem;
            padding: 0.75rem 1.5rem;
        }
        .gradient-custom-2 {
            background: linear-gradient(45deg, #667eea, #764ba2);
        }
    </style>
{% endblock %}

{% block content %}
<section class="gradient-form d-flex justify-content-center align-items-center">
    <div class="container">
        <div class="row d-flex justify-content-center align-items-center h-100">
            <div class="col-xl-10">
                <div class="card rounded-3 text-black">
                    <div class="row g-0">
                        <div class="col-lg-6">
                            <div class="card-body p-md-5 mx-md-4">
                                <div class="text-center">
                                    <img src="{{ asset('/uploads/images/ASTRASO.png') }}" class="mb-5 w-50" alt="Logo Astraso">
                                </div>

                                <div class="text-center">
                                    <h4 class="mb-4">Créer un compte</h4>
                                </div>

                                {{ form_start(registrationForm) }}

                                <div class="form-outline mb-4">
                                    {{ form_widget(registrationForm.email, { 'attr': { 'class': 'form-control', 'placeholder': 'Adresse e-mail' } }) }}
                                </div>

                                <div class="form-outline mb-4">
                                    {{ form_widget(registrationForm.username, { 'attr': { 'class': 'form-control', 'placeholder': 'Nom d\'utilisateur' } }) }}
                                </div>

                                <div class="form-outline mb-4">
                                    {{ form_widget(registrationForm.plainPassword, { 'attr': { 'class': 'form-control', 'placeholder': 'Mot de passe' } }) }}
                                </div>

                                <div class="form-check d-flex justify-content-center mb-4">
                                    {{ form_widget(registrationForm.agreeTerms, { 'attr': { 'class': 'form-check-input' } }) }}
                                    {{ form_label(registrationForm.agreeTerms, 'J\'accepte les termes et conditions', { 'label_attr': { 'class': 'form-check-label' } }) }}
                                </div>

                                <div class="text-center pt-1 mb-5 pb-1">
                                    <button type="submit" class="btn btn-primary btn-lg btn-block">Créer un compte</button>
                                    
                                </div>

                                <div class="text-center">
                                    <p class="text-muted mb-0">Déjà un compte? <a href="{{ path('app_login') }}" class="text-dark fw-bold">Connectez-vous ici</a></p>
                                </div>

                                {{ form_end(registrationForm) }}
                            </div>
                        </div>

                        <div class="col-lg-6 gradient-custom-2 d-flex align-items-center">
                            <div class="text-white px-3 py-4 p-md-5 mx-md-4">
                                <h4 class="mb-4">Gardez une trace de vos retards en toute simplicité.</h4>
                                <p class="small mb-0">Astraso est une application qui vous permet de signaler facilement vos retards à votre équipe. Avec une interface simple et intuitive, vous pouvez rapidement saisir la raison de votre retard et le communiquer à vos collègues. Astraso vous aide à rester organisé et à maintenir une communication ouverte au sein de votre équipe.</p>
                            </div>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
>>>>>>> css-styles
{% endblock %}
